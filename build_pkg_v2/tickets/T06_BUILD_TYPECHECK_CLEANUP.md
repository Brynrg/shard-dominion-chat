# T06 — Build and Typecheck Cleanup

## Goal
Stabilize dev workflow.

## Tasks
- Run npm install
- Run typecheck
- Run build
- Fix strict TypeScript errors
- Remove unused/duplicated code
- Ensure CI commands match package scripts

## Acceptance
- `npm run typecheck` passes
- `npm run build` passes
- no obvious console errors on load
